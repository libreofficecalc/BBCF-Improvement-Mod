# Research Artifacts

This subtree contains long reverse-engineering notes, generated reports, and scripts. Do not read broad files by default.

- Prefer exact `rg` searches, then small `sed` slices.
- `RankedProgress.md`, `*GhidraReport.txt`, and `Tadatys-BBCF-Ghidra/BBCF.h` can be large. Open only relevant sections.
- Generated Ghidra DB folder `BBCF-Ghidra-Project/` is local-only and ignored.
- For current replay takeover snapshot work, use `docs/replay_takeover/` first, not this folder.
