# Vendored Dependencies

Avoid reading or editing this subtree unless task explicitly concerns vendored code, SDK headers, or wrapper compatibility.

Use targeted search only, e.g. `rg --no-ignore "ISteamNetworking" depends/steam_api/public/steam`. Do not broad-scan `depends/`; it is large and mostly third-party.
